import SectionOverview from "@/components/SectionOverview";

export const metadata = {
  title: "Creating multi-parameter functions — loop",
  description:
    "Build real multi-parameter functions: BMI, triangles, factorials and Fibonacci — then learn recursion.",
};

export default function MultiParamOverview() {
  return <SectionOverview slug="multi-parameter" />;
}
