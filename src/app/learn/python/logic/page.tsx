import SectionOverview from "@/components/SectionOverview";

export const metadata = {
  title: "Logic and bit operations — loop",
  description:
    "Logical operators (and, or, not), bitwise operators (&, |, ^, ~), bit masks and shifting.",
};

export default function LogicOverview() {
  return <SectionOverview slug="logic" />;
}
