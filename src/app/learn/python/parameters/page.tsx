import SectionOverview from "@/components/ui/SectionOverview";

export const metadata = {
  title: "How functions communicate with their environment — loop",
  description:
    "Parameters and arguments: how to pass data into a function, by position and by keyword, and how to mix the two.",
};

export default function ParametersOverview() {
  return <SectionOverview slug="parameters" />;
}
