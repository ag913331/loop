import SectionOverview from "@/components/ui/SectionOverview";

export const metadata = {
  title: "Exceptions — loop",
  description:
    "Catch and handle runtime errors with try/except, deal with several kinds at once, meet the common exceptions, and learn to test and debug.",
};

export default function ExceptionsOverview() {
  return <SectionOverview slug="exceptions" />;
}
