import SectionOverview from "@/components/SectionOverview";

export const metadata = {
  title: "Sorting lists: bubble sort — loop",
  description:
    "The bubble sort algorithm, shown step by step, and Python's built-in sort() and sorted().",
};

export default function SortingOverview() {
  return <SectionOverview slug="sorting" />;
}
