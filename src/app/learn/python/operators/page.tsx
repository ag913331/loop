import SectionOverview from "@/components/SectionOverview";

export const metadata = {
  title: "Operators — loop",
  description:
    "Python's data-manipulation tools: the arithmetic operators, the int/float rules, and operator precedence.",
};

export default function OperatorsOverview() {
  return <SectionOverview slug="operators" />;
}
