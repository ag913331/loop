import SectionOverview from "@/components/SectionOverview";

export const metadata = {
  title: "Interaction with the user — loop",
  description:
    "Read what the user types with input(), understand why it's always a string, and convert it with type casting.",
};

export default function InputOverview() {
  return <SectionOverview slug="input" />;
}
