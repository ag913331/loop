import SectionOverview from "@/components/ui/SectionOverview";

export const metadata = {
  title: "Making decisions in Python — loop",
  description:
    "Comparison operators and conditional execution: ask yes/no questions and run code depending on the answer.",
};

export default function DecisionsOverview() {
  return <SectionOverview slug="decisions" />;
}
