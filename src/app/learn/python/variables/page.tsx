import SectionOverview from "@/components/SectionOverview";

export const metadata = {
  title: "Variables — loop",
  description:
    "Named boxes that store values: naming rules, creating and using variables, reassignment, and shortcut operators.",
};

export default function VariablesOverview() {
  return <SectionOverview slug="variables" />;
}
