import ModuleOverview from "@/components/ModuleOverview";

export const metadata = {
  title: "Introduction to Python — loop",
  description:
    "Meet the language: where Python came from, what makes it special, its rivals, where it runs, its trade-offs, and its many implementations.",
};

export default function MeetPythonOverview() {
  return <ModuleOverview slug="meet-python" />;
}
