import SectionOverview from "@/components/SectionOverview";

export const metadata = {
  title: "Scopes in Python — loop",
  description:
    "Local and global scope, the global keyword, and how a function interacts with the arguments you pass it.",
};

export default function ScopesOverview() {
  return <SectionOverview slug="scopes" />;
}
