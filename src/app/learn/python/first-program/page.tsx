import SectionOverview from "@/components/SectionOverview";

export const metadata = {
  title: "Your very first program — loop",
  description:
    "Write a real little Python program that remembers a value and uses it.",
};

export default function FirstProgramOverview() {
  return <SectionOverview slug="first-program" />;
}
