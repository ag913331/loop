import SectionOverview from "@/components/ui/SectionOverview";

export const metadata = {
  title: "Working with strings — loop",
  description:
    "Index and slice strings, reshape them with methods, and build text with f-strings.",
};

export default function StringsOverview() {
  return <SectionOverview slug="strings" />;
}
