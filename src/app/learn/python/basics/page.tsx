import SectionOverview from "@/components/SectionOverview";

export const metadata = {
  title: "Basics — loop",
  description:
    "Your first real programs and a thorough tour of print() — the function you'll use to see what your code does.",
};

export default function BasicsOverview() {
  return <SectionOverview slug="basics" />;
}
