import SectionOverview from "@/components/SectionOverview";

export const metadata = {
  title: "Function return — loop",
  description:
    "Effects vs results, the return statement, the None value, lists in and out of functions, and five worked examples.",
};

export default function ReturnOverview() {
  return <SectionOverview slug="return-values" />;
}
