import SectionOverview from "@/components/ui/SectionOverview";

export const metadata = {
  title: "Tuples and dictionaries — loop",
  description:
    "Mutability, tuples, dictionaries, dictionary methods, and how tuples and dictionaries work together.",
};

export default function TuplesDictsOverview() {
  return <SectionOverview slug="tuples-dicts" />;
}
