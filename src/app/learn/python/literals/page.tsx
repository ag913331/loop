import SectionOverview from "@/components/ui/SectionOverview";

export const metadata = {
  title: "Python literals — loop",
  description:
    "The raw values you write straight into code: integers, floats, strings and booleans, and the type Python gives each.",
};

export default function LiteralsOverview() {
  return <SectionOverview slug="literals" />;
}
