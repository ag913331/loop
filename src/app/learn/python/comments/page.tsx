import SectionOverview from "@/components/ui/SectionOverview";

export const metadata = {
  title: "Comments — loop",
  description:
    "Notes for humans that Python ignores: why and when to use comments, how to write them, and commenting out code.",
};

export default function CommentsOverview() {
  return <SectionOverview slug="comments" />;
}
