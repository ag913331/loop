import SectionOverview from "@/components/SectionOverview";

export const metadata = {
  title: "Loops in Python — loop",
  description:
    "while and for loops, the range() function, break and continue — everything you need to make code repeat.",
};

export default function LoopsOverview() {
  return <SectionOverview slug="loops" />;
}
