import SectionOverview from "@/components/ui/SectionOverview";

export const metadata = {
  title: "Functions — loop",
  description:
    "Why functions exist, how decomposition tames complexity, where functions come from, and how to define and call your own.",
};

export default function FunctionsOverview() {
  return <SectionOverview slug="functions" />;
}
