import SectionOverview from "@/components/SectionOverview";

export const metadata = {
  title: "Lists and list processing — loop",
  description:
    "Python lists: indexing, slicing, changing, adding and removing elements, and processing them with loops.",
};

export default function ListsOverview() {
  return <SectionOverview slug="lists" />;
}
